define Selection {
    obj:{},
    constructor : function (obj) {
        this.text = obj.text;
        this.id = obj.id;
        this.rules = obj.rules;   
        this.html = '';
    }        

}
rule Hello {
    
    when {           
        m : Selection m.id =~ /^rule00001$/
    }
    then {
        modify(m, function(){              
            this.html = formatSpan(this.id, this.rules, this.text);
            this.id = getId(this.id);
        });
        
        
    }
}